apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: lldapusers.lldap-operator.lukidoescode.com
spec:
  group: lldap-operator.lukidoescode.com
  names:
    categories: []
    kind: LldapUser
    plural: lldapusers
    shortNames:
    - llu
    singular: lldapuser
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.username
      name: Username
      type: string
    - jsonPath: .spec.email
      name: Email
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for LldapUserSpec via `CustomResource`
        properties:
          spec:
            properties:
              attributes:
                items:
                  properties:
                    name:
                      type: string
                    value:
                      items:
                        type: string
                      type: array
                  required:
                  - name
                  - value
                  type: object
                nullable: true
                type: array
              displayName:
                nullable: true
                type: string
              email:
                maxLength: 320
                minLength: 3
                pattern: ^[^@\s]+@[^@\s]+\.[^@\s]+$
                type: string
              firstName:
                nullable: true
                type: string
              lastName:
                nullable: true
                type: string
              passwordPolicy:
                default: Manage
                enum:
                - Manage
                - InitialOnly
                - Ignore
                type: string
              passwordSecretRef:
                nullable: true
                properties:
                  key:
                    type: string
                  name:
                    type: string
                required:
                - key
                - name
                type: object
              username:
                maxLength: 64
                minLength: 1
                pattern: ^[a-zA-Z0-9._-]+$
                type: string
            required:
            - email
            - username
            type: object
          status:
            nullable: true
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating details about the transition. This may be an empty string.
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                nullable: true
                type: array
              observedGeneration:
                format: int64
                nullable: true
                type: integer
              passwordHash:
                nullable: true
                type: string
              uuid:
                nullable: true
                type: string
            type: object
        required:
        - spec
        title: LldapUserValidated
        type: object
        x-kubernetes-validations:
        - message: metadata.labels must include 'lldap-operator.lukidoescode.com/lldap-instance' with a non-empty value
          reason: FieldValueRequired
          rule: has(self.metadata.labels) && 'lldap-operator.lukidoescode.com/lldap-instance' in self.metadata.labels && size(self.metadata.labels['lldap-operator.lukidoescode.com/lldap-instance']) > 0
        - message: passwordSecretRef is required unless passwordPolicy is Ignore
          reason: FieldValueRequired
          rule: self.spec.passwordPolicy == 'Ignore' || has(self.spec.passwordSecretRef)
    served: true
    storage: true
    subresources:
      status: {}
