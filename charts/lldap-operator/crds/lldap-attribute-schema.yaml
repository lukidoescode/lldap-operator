apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: lldapattributeschemas.lldap-operator.lukidoescode.com
spec:
  group: lldap-operator.lukidoescode.com
  names:
    categories: []
    kind: LldapAttributeSchema
    plural: lldapattributeschemas
    shortNames:
    - llas
    singular: lldapattributeschema
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.attributeName
      name: Name
      type: string
    - jsonPath: .spec.attributeType
      name: Type
      type: string
    - jsonPath: .spec.target
      name: Target
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for LldapAttributeSchemaSpec via `CustomResource`
        properties:
          spec:
            properties:
              attributeName:
                maxLength: 64
                minLength: 1
                pattern: ^[a-zA-Z][a-zA-Z0-9_]*$
                type: string
              attributeType:
                enum:
                - String
                - Integer
                - JpegPhoto
                - DateTime
                type: string
              isEditable:
                default: false
                type: boolean
              isList:
                default: false
                type: boolean
              isVisible:
                default: false
                type: boolean
              target:
                enum:
                - User
                - Group
                type: string
            required:
            - attributeName
            - attributeType
            - target
            type: object
          status:
            nullable: true
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating details about the transition. This may be an empty string.
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                nullable: true
                type: array
              observedGeneration:
                format: int64
                nullable: true
                type: integer
            type: object
        required:
        - spec
        title: LldapAttributeSchemaValidated
        type: object
        x-kubernetes-validations:
        - message: metadata.labels must include 'lldap-operator.lukidoescode.com/lldap-instance' with a non-empty value
          reason: FieldValueRequired
          rule: has(self.metadata.labels) && 'lldap-operator.lukidoescode.com/lldap-instance' in self.metadata.labels && size(self.metadata.labels['lldap-operator.lukidoescode.com/lldap-instance']) > 0
    served: true
    storage: true
    subresources:
      status: {}
